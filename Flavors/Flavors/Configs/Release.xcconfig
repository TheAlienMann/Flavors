#include "Base.xcconfig"

APP_NAME = $(inherited)

//PRIMARY_APPICON_SET_NAME = $(inherited)
ASSETCATALOG_COMPILER_APPICON_NAME = $(inherited)
//ASSETCATALOG_COMPILER_PRIMARY_APPICON_SET_NAME = $(inherited)

PRODUCT_BUNDLE_IDENTIFIER = $(inherited)

BASE_URL = $(inherited)

API_KEY = $(inherited)
