#include "Base.xcconfig"

APP_NAME = $(inherited)-Dev

ASSETCATALOG_COMPILER_APPICON_NAME = $(inherited)-NonProd

PRODUCT_BUNDLE_IDENTIFIER = $(inherited).debug

//BASE_URL = dev.$(inherited)

//API_KEY = $(inherited) // you probably have a different api key for debugging/development.
